CREATE TABLE [dbo].[week_day_name] (
    [number]     INT          NOT NULL,
    [name]       VARCHAR (30) NOT NULL,
    [abrev]      VARCHAR (3)  NOT NULL,
    [country_id] INT          NOT NULL
);

