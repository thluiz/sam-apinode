CREATE TABLE [dbo].[enum_branch_category] (
    [id]          INT          NOT NULL,
    [description] VARCHAR (50) NULL,
    [abrev]       VARCHAR (10) NULL,
    PRIMARY KEY CLUSTERED ([id] ASC)
);

